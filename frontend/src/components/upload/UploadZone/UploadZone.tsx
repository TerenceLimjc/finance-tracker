/**
 * UploadZone
 *
 * Ant Design Upload.Dragger component for drag-and-drop bank statement upload.
 * Accepts: PDF, CSV, Excel, OFX, QIF — max 50 MB per file.
 * On successful drop/select, calls useUploadFile mutation.
 */
export function UploadZone() {
  // TODO: implement — see docs/ui-design-spec.md Screen 2 Upload Zone
  return null;
}
