/**
 * MonthSelector
 *
 * Left/right arrow buttons to step prev/next month.
 * Center label opens an Ant Design Select dropdown for arbitrary month jump.
 *
 * Reads and writes selectedMonth from filterStore.
 */
export function MonthSelector() {
  // TODO: implement — see docs/ui-design-spec.md Month navigation notes
  return null;
}
