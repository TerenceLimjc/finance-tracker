/**
 * CategoryPieChart
 *
 * Renders the Spending by Category pie chart using @ant-design/charts Pie.
 * Legend entries show: category name, dollar total, and percentage.
 * Clicking a slice or legend row calls onCategoryClick to filter the
 * transaction table below.
 *
 * Props:
 *  - data: CategorySpending[]
 *  - activeCategoryId: number | null  (selected slice)
 *  - onCategoryClick: (categoryId: number) => void
 */
export function CategoryPieChart() {
    // TODO: implement — see docs/ui-design-spec.md "Spending by Category" section
    return null;
}
